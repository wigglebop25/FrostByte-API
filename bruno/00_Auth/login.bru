meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: https://127.0.0.1:3000/api/v1/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "adminuser",
    "password": "adminpassword"
  }
}

script:post-response {
  let data = res.getBody();
  if (data.token) {
    bru.setVar("token", data.token);
  }
}
